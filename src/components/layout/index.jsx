import React from 'react';

import Sidebar from './sidebar';

function Layout() {
    return (
        <div style={{ height: '100vh' }}>
            <Sidebar />
        </div>
    );
}

export default Layout;
